import model_labels.label_GestureCommand
import model_labels.label_GestureOnly
import model_labels.label_GestureCommand_35


label_map = {
    'GestureOnly': label_GestureOnly.labels_map,
    'GestureCommand': label_GestureCommand.labels_map,
    'GestureCommand35': label_GestureCommand_35.labels_map
}
